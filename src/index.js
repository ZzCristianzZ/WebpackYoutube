// Ejemplo 1 De como Webpack traspila el codigo minimizandolo
const saludo = "Hola esta es una prueba de como transpila codigo webpack";
console.log("saludo");

// Ejemplo del desarrollo del Babel-loader

const arr = [1,2,3];
codeEsNext = () => console.log(...arr);

codeEsNext();

import style from "./style.css";